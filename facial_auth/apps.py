from django.apps import AppConfig

class FacialAuthConfig(AppConfig):  # Le nom doit correspondre
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'facial_auth'  # Doit correspondre au nom du dossier